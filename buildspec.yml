version: 0.2

phases:
  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $621753709886.dkr.ecr.us-east-1.amazonaws.com/springboot-app
      - IMAGE_TAG=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c 1-7)
  build:
    commands:
      - echo Building the Docker image...
      - mvn clean package -DskipTests
      - docker build -t $ECR_REPO_URI:latest .
      - docker tag $ECR_REPO_URI:latest $ECR_REPO_URI:$IMAGE_TAG
  post_build:
    commands:
      - echo Pushing the Docker image...
      - docker push $ECR_REPO_URI:latest
      - docker push $ECR_REPO_URI:$IMAGE_TAG
      - printf '[{"name":"springboot-container","imageUri":"%s"}]' $621753709886.dkr.ecr.us-east-1.amazonaws.com/springboot-app:latest > imagedefinitions.json

artifacts:
  files:
    - imagedefinitions.json
